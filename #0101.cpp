#include <iostream>

int main() {
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);
    int t, int a[4];
    long long n;
    std::cin >> t;
    for (int i = 0; i < t; i++)
    {
        std::cin >> n;
        for (int i = 0; i < 4; i++)
        {
            std::cin >> a[i];
            
        }
        
    }
    
    return 0;
}